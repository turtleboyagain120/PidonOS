apiVersion: v1
kind: Service
metadata:
  name: pidonos
  labels:
    app: pidonos
spec:
  type: ClusterIP         # change to LoadBalancer for public cloud
  selector:
    app: pidonos
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
